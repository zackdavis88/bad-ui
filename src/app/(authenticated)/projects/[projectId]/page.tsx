import { Metadata } from 'next';
import { ProjectDetailsPageContent } from '@/app/components/project-details-page';

export const metadata: Metadata = {
  title: 'Project Details',
};

export default ProjectDetailsPageContent;
