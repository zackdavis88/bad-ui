import { Metadata } from 'next';
import { ProjectMembershipsPageContent } from '@/app/components/project-memberships-page';

export const metadata: Metadata = {
  title: 'Project Memberships',
};

export default ProjectMembershipsPageContent;
